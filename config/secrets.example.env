###############################################
# Secure Configuration Template
# ---------------------------------------------
# 請複製此檔案為 `.env` 或 `config/secrets.env`
# 並填入實際的敏感資訊。這些值會被 .gitignore 排除，
# 請勿將實際內容提交至 GitHub。
###############################################

# === LINE Messaging API ===
# LINE 官方帳號資訊：用於週報推播
LINE_CHANNEL_ID=2007362664                         # Channel ID (僅示意)
LINE_CHANNEL_SECRET=your_line_channel_secret       # 務必更新為重新產生的 secret
LINE_CHANNEL_ACCESS_TOKEN=your_line_access_token   # 長期/短期 access token

# 內部測試目標：可放 userId 或 groupId（逗號分隔多個）
LINE_TARGET_IDS=Uxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
LINE_DASHBOARD_URL=https://lookerstudio.google.com/...

# === Google Chat (SIT/UAT 專用) ===
GOOGLE_CHAT_WEBHOOK=https://chat.googleapis.com/v1/spaces/.../messages?key=...&token=...

# === SendGrid / SMTP ===
SENDGRID_API_KEY=your_sendgrid_api_key
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=smtp_user
SMTP_PASSWORD=smtp_password

# === GCP ===
GOOGLE_CLOUD_PROJECT=datalake360-saintpaul
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json  # 若使用 ADC 可留空

# === 其他（依需求補充） ===
# 例如：BigQuery 資料集、Slack Webhook、Datadog API Key 等
# BQ_DATASET=...
# SLACK_WEBHOOK_URL=...
# DATADOG_API_KEY=...

